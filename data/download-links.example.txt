# Una URL de manifiesto JSON por línea. Se ignoran líneas vacías y comentarios tras '#'.
# El JSON debe ser un array o un objeto con games[]. Cada entrada requiere platform y url.
# Campos opcionales: gameId, sourceId, name, sizeBytes, checksum y available.
# Solo se aceptan URLs HTTPS/HTTP autorizadas para el manifiesto y para cada juego.
# https://example.org/emubox/homebrew-manifest.json